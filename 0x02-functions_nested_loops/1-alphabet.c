#include "main.h"

/**
 * print_alphabet - prints the alphabet in lower case
 * Return : nothing
 */

void print_alphabet(void) {
	for(char i = 'a'; i <= 'z' ; ++i) {
		_putchar(i);
	}
	_putchar('\n');
}
